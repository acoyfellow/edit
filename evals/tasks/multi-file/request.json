{
  "version": 1,
  "workspace_root": "evals/tasks/multi-file/workspace",
  "operations": [
    {
      "type": "replace_text",
      "path": "lib/counter.ex",
      "expected": "defmodule Counter do\n  def next(n), do: n + 1\nend\n",
      "replacement": "defmodule Counter do\n  def next(n), do: n + 2\nend\n"
    },
    {
      "type": "replace_text",
      "path": "test/counter_test.exs",
      "expected": "defmodule CounterTest do\n  use ExUnit.Case\n  test \"next\", do: assert Counter.next(1) == 2\nend\n",
      "replacement": "defmodule CounterTest do\n  use ExUnit.Case\n  test \"next\", do: assert Counter.next(1) == 3\nend\n"
    }
  ],
  "verification": {
    "type": "file_equals",
    "path": "lib/counter.ex",
    "expected": "defmodule Counter do\n  def next(n), do: n + 2\nend\n"
  },
  "capabilities": [
    "workspace.read",
    "workspace.write"
  ],
  "permissions": {
    ".": [
      "replace_text"
    ]
  }
}
